"""Streamlit entrypoint (screens to be implemented per project spec)."""

# import streamlit as st

if __name__ == "__main__":
    raise SystemExit(
        "Run with: streamlit run ui/app.py (from race_rc_project after implementing UI)."
    )

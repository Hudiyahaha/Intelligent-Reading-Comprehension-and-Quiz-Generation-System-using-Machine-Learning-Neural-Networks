# RACE Reading Comprehension — core stack (pin loosely for reproducibility)
pandas>=2.0.0,<3
numpy>=1.24.0,<3
scipy>=1.11.0,<2
scikit-learn>=1.3.0,<2
joblib>=1.3.0,<2
pyarrow>=14.0.0,<22
tqdm>=4.66.0,<5
pytest>=7.4.0,<10
nltk>=3.8.1,<4
rouge-score>=0.1.2,<1

# BERT comparison baseline (Model A / Model B rerank). Install torch from the
# CPU index when no GPU is available:
#   pip install "torch==2.*" --index-url https://download.pytorch.org/whl/cpu
torch>=2.0,<3
transformers>=4.41,<5

# UI & notebooks (install when needed)
streamlit>=1.28.0,<2
jupyter>=1.0.0,<2
